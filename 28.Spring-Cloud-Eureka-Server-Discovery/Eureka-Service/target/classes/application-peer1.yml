server:
  port: 8080

spring:
  application:
    name: Eureka-Server

eureka:
  instance:
    hostname: localhost
  client:
    serviceUrl:
      defaultZone: http://mrbird:123456@localhost:8080/eureka/
  server:
    enable-self-preservation: false

